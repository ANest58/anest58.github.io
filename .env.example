# Hugging Face Fit Agent (optional)
#
# Free Inference Providers token (Inference Providers permission):
# https://huggingface.co/settings/tokens/new?ownUserPermissions=inference.serverless.write&tokenType=fineGrained
#
# PUBLIC_HF_TOKEN is embedded in the static build — only use for a personal demo.
# Prefer pasting the token in the Ask page UI (browser localStorage) or hosting hf-space/.
#
# PUBLIC_HF_TOKEN=
# PUBLIC_HF_MODEL=Qwen/Qwen2.5-7B-Instruct:fastest
# PUBLIC_HF_SPACE_ID=your-username/portfolio-fit-agent
